trame:
  cmd:
    - python
    - /deploy/cone.py
    - --host
    - ${host}
    - --port
    - ${port}
    - --authKey
    - ${secret}
    - --server

cone:
  cmd:
    - python
    - /deploy/cone.py
    - --host
    - ${host}
    - --port
    - ${port}
    - --authKey
    - ${secret}
    - --server

sphere:
  cmd:
    - python
    - /deploy/sphere.py
    - --host
    - ${host}
    - --port
    - ${port}
    - --authKey
    - ${secret}
    - --server